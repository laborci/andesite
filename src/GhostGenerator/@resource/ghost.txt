<?php namespace {{ghost-namespace}};

use Andesite\Ghost\Model;
use {{shadow-namespace}}\__{{name}};

/**
 * @database default
 * @table {{table}}
 * @mutable true
 */
class {{name}} extends __{{name}}{

	static protected function extendModel(Model $model):Model{
		return $model;
	}

}